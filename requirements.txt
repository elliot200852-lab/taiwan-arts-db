# scripts/pull_content.py 依賴（從 Drive 拉全站內容）
# 安裝：python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
PyYAML>=6.0
google-api-python-client>=2.100
google-auth>=2.23

# scripts/verify_live.py 只用標準庫，無額外依賴
